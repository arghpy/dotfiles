#!/usr/bin/env bash

ICON=""

DATE=$(date '+%b %d (%a) %H:%M')

printf "%s  %s" "$ICON" "$DATE"

case $BUTTON in
    1)
        dunstify "$ICON $DATE"
        ;;
    6)
        st -e nvim $(which $0)
        ;;
esac
